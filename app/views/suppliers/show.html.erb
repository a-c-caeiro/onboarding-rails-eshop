  <% content_for :navigation do %>
  <div class="navLeft">
    <span style="color: #6A63D9"><i class="fas fa-bolt"></i></span>
    I dont really know your productivity score, but welcome:
    <span style="color: black"><%= @supplier.name%></span>
  </div>

  <div class="navRight">
    <i class="fas fa-comments"></i> chats
    <i class="fas fa-sliders-h"></i> settings
  </div>
<% end %>
 
<h2>Orders
	<select name="Status" id="cars" class="dropdown">
  <option value="volvo">Volvo</option>
  <option value="saab">Saab</option>
  <option value="mercedes">Mercedes</option>
  <option value="audi">Audi</option>
</select>
<select name="Date" id="cars" class="dropdown">
  <option value="volvo">Volvo</option>
  <option value="saab">Saab</option>
  <option value="mercedes">Mercedes</option>
  <option value="audi">Audi</option>
</select>
    <%= search_field_tag :search, params[:search], placeholder: "Search", class: "searchbar" %>  

</h2>

<table>
<thead>
  <tr>
    <th>#</th>
    <th>order id</th>
    <th>created</th>
    <th>customer</th>
    <th>total</th>
    <th>profit</th>
    <th>status</th>
    <th></th>
  </tr>
</thead>
<tbody>
	<% @orderssupplier.each do |ord| %>

  <tr id = "tr<%= ord.id%>" class = "solid">
    <td><input type="checkbox" id="vehicle1" name="vehicle1" value="Bike"></td>
    <td><%= ord.id %></td>
    <td><%= ((Time.now - ord.created_at)/ 1.minute).round %> min ago</td>
    <td>
      <% customer = ord.customer %>
    	<%= customer.name %>
    	<span class="icon"><i class="fas fa-star"></i></span>	
    	<%= customer.rating	 %> 
    	</td>
    <td> € <%= ord.total_price %> </td>
    <td> €0 <span class=label> 0% </span></td>
    <td> <span class=label><%= ord.status %> </span></td>
    <td style="width: 7%"> 
      <button type="button" class="buttonDown"data-id="<%= ord.id%>" class="collapsible"value="">
        <i class="fas fa-caret-down"></i>
      </button>
    </td>
  </tr>
  <tr class="contentcolapse" id="detail-<%= ord.id%>" mydata="wowMuchEmpty">
  </tr>
  <% end %>
</tbody>
</table>

<% content_for :sidebar do %>
  <p>OVERVIEW</p>
  <p>Total Orders</p>
  <h2><%=@orderssupplier.count%></h2>
  <p>Need to implement:</p>
  <ul>
    <li> Orders today</li>
    <li> pending orders</li>
    <li> Delivererd</li>
    <li> Cancelled</li>
  </ul>

<% end %>